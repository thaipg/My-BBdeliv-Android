<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.ToolBarChildStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColorPrimary">@color/colorGreyAndroid</item>
        <item name="android:textColorSecondary">@color/colorGreyAndroid</item>
        <item name="actionMenuTextColor">@color/colorGreyAndroid</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextDrawableLeft">
        <item name="android:drawablePadding">5dp</item>
    </style>

    <style name="Toolbar">
        <item name="android:theme">@style/Album.Theme.Toolbar</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?actionBarSize</item>
        <item name="android:background">?colorPrimary</item>
        <item name="navigationIcon">@drawable/ic_back_white</item>
        <item name="titleTextColor">@color/albumFontLight</item>
        <item name="subtitleTextColor">@color/albumFontLight</item>
        <item name="popupTheme">@style/Album.Theme.Toolbar.Popup</item>
    </style>
</resources>
